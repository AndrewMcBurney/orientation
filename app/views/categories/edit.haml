- @category.errors.full_messages.each do |error|
  %p.bulletin.bulletin--warning= error

%section.row
  .cell.well.well--l

    = render partial: 'form', locals: { label_text: 'Edit Team', button_text: 'Save' }
