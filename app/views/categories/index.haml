%section.row
  .cell.well.well--l

    = render layout: 'articles/search', locals: { alternative_content: true, hide_results: true } do
      = render 'categories/categories', categories: @categories
