.row.row--highlight.row--pattern
  .cell.well.well--l

    %h1.tci.tac= @category.label

%section.row
  .cell.well.well--l
    = render 'categories/guides', guides: @guides
    = link_to 'Edit', edit_category_url( @category ), class: 'btn btn--a',
      data: { shortcut: 'e' }
